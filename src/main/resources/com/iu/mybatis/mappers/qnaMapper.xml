<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="qnaMapper">
  
  	<!-- 전체조회 -->
	<select id="boardList" resultType="com.iu.s4.model.BoardQnaVO" parameterType="com.iu.s4.util.Pager">
		select * from
			(select N.*, rownum R from 
				(select * from qna 
				order by num desc) N)
		where R between #{startRow} and #{lastRow}
	</select>
  
	<!-- 글전체 수 -->
	<select id="boardCount" resultType="java.lang.Integer" parameterType="com.iu.s4.util.Pager">
		select count(num) from qna
		<choose>
			<when test="kind == 'kt'">
				where title like '%'||#{search}||'%'
			</when>
			<when test="kind == 'kc'">
				where contents like '%'||#{search}||'%'
			</when>
			<otherwise>
				where writer like '%'||#{search}||'%'
			</otherwise>			
		</choose>
	
	</select>
  
  </mapper>